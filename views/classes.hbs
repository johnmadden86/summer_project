{{> menu id="classes"}}
<section class="ui segment">

<div class="ui styled fluid accordion" id="accordion">
    {{#each classes}}
        {{> class-title}}
        {{> class-content}}
    {{/each}}
</div>
</section>
<script>
    $( function () {
        $(' #accordion ').accordion();
    } );
</script>
