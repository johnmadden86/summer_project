{{> menu id="classes"}}

<div class="ui styled fluid accordion" id="accordion">
    {{#each classes}}
        {{> class-title}}
        {{> class-content}}
    {{/each}}
</div>
<script>
    $( function () {
        $(' #accordion ').accordion();
    } );
</script>
